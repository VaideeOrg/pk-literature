name: Terraform Plan

# spec-12-cicd.md — PR pipeline: fmt/validate/plan only, never apply.
#
# Single-environment posture: this repo only stands up one real AWS
# environment ("prod" — terraform/environments/prod), not the
# dev/qa/prod split terraform/environments/{dev,qa} were originally
# scaffolded for. Those two directories are left in place (harmless,
# useful if multi-environment is ever revisited) but CI never plans or
# applies them — this workflow is hardcoded to prod rather than
# matrixing over whichever environment a PR happens to touch.

on:
  pull_request:
    branches:
      - main
    paths:
      - "terraform/modules/**"
      - "terraform/environments/prod/**"
      - ".github/workflows/terraform-*.yml"

permissions:
  id-token: write # OIDC — no long-lived AWS keys, infrastructure/iam.md
  contents: read
  pull-requests: write # to comment the plan output on the PR

jobs:
  plan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Every environments/prod/*.tf file reads its own service's
      # deployment package hash via filebase64sha256() — that's
      # evaluated during `plan`, not just `apply`, so every package
      # must exist before terraform runs at all, same as
      # terraform-apply.yml.
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"
      - run: pnpm install --frozen-lockfile

      # Each service's build previously ran unconditionally on every
      # single invocation, regardless of whether its own source (or the
      # shared packages it depends on) actually changed — real CI time
      # wasted on identical rebuilds for infra-only PRs that never
      # touched any app code. actions/cache keys on a hash of exactly
      # the inputs that affect that service's build output (its own
      # src/package.json/tsconfig, the workspace packages it imports
      # from, packages/tsconfig's shared base config, and the lockfile)
      # — an exact key match means the source is provably identical to
      # a previous successful build, so the cached zip is restored and
      # the build step skipped entirely (gated by cache-hit below);
      # anything else runs the build fresh, same as before, and caches
      # the new result under its new key.
      - name: Cache api-catalog Lambda package
        id: cache-api-catalog
        uses: actions/cache@v4
        with:
          path: apps/api-catalog/dist-lambda.zip
          key: api-catalog-lambda-${{ hashFiles('apps/api-catalog/src/**', 'apps/api-catalog/package.json', 'apps/api-catalog/tsconfig*.json', 'packages/domain-types/src/**', 'packages/domain-types/package.json', 'packages/contracts/src/**', 'packages/contracts/package.json', 'packages/tsconfig/**', 'pnpm-lock.yaml') }}
      - name: Build api-catalog Lambda package
        if: steps.cache-api-catalog.outputs.cache-hit != 'true'
        run: bash scripts/package-lambda.sh
        working-directory: apps/api-catalog

      - name: Cache api-publisher-import Lambda package
        id: cache-api-publisher-import
        uses: actions/cache@v4
        with:
          path: apps/api-publisher-import/dist-lambda.zip
          key: api-publisher-import-lambda-${{ hashFiles('apps/api-publisher-import/src/**', 'apps/api-publisher-import/package.json', 'apps/api-publisher-import/tsconfig*.json', 'packages/adapter-sdk/src/**', 'packages/adapter-sdk/package.json', 'packages/contracts/src/**', 'packages/contracts/package.json', 'packages/tsconfig/**', 'pnpm-lock.yaml') }}
      - name: Build api-publisher-import Lambda package
        if: steps.cache-api-publisher-import.outputs.cache-hit != 'true'
        run: bash scripts/package-lambda.sh
        working-directory: apps/api-publisher-import

      - name: Cache api-feed Lambda package
        id: cache-api-feed
        uses: actions/cache@v4
        with:
          path: apps/api-feed/dist-lambda.zip
          key: api-feed-lambda-${{ hashFiles('apps/api-feed/src/**', 'apps/api-feed/package.json', 'apps/api-feed/tsconfig*.json', 'packages/domain-types/src/**', 'packages/domain-types/package.json', 'packages/contracts/src/**', 'packages/contracts/package.json', 'packages/tsconfig/**', 'pnpm-lock.yaml') }}
      - name: Build api-feed Lambda package
        if: steps.cache-api-feed.outputs.cache-hit != 'true'
        run: bash scripts/package-lambda.sh
        working-directory: apps/api-feed

      - name: Cache api-search Lambda package
        id: cache-api-search
        uses: actions/cache@v4
        with:
          path: apps/api-search/dist-lambda.zip
          key: api-search-lambda-${{ hashFiles('apps/api-search/src/**', 'apps/api-search/package.json', 'apps/api-search/tsconfig*.json', 'packages/domain-types/src/**', 'packages/domain-types/package.json', 'packages/contracts/src/**', 'packages/contracts/package.json', 'packages/tsconfig/**', 'pnpm-lock.yaml') }}
      - name: Build api-search Lambda package
        if: steps.cache-api-search.outputs.cache-hit != 'true'
        run: bash scripts/package-lambda.sh
        working-directory: apps/api-search

      - name: Cache api-commerce Lambda package
        id: cache-api-commerce
        uses: actions/cache@v4
        with:
          path: apps/api-commerce/dist-lambda.zip
          key: api-commerce-lambda-${{ hashFiles('apps/api-commerce/src/**', 'apps/api-commerce/package.json', 'apps/api-commerce/tsconfig*.json', 'packages/domain-types/src/**', 'packages/domain-types/package.json', 'packages/contracts/src/**', 'packages/contracts/package.json', 'packages/tsconfig/**', 'pnpm-lock.yaml') }}
      - name: Build api-commerce Lambda package
        if: steps.cache-api-commerce.outputs.cache-hit != 'true'
        run: bash scripts/package-lambda.sh
        working-directory: apps/api-commerce

      - name: Cache api-identity Lambda package
        id: cache-api-identity
        uses: actions/cache@v4
        with:
          path: apps/api-identity/dist-lambda.zip
          key: api-identity-lambda-${{ hashFiles('apps/api-identity/src/**', 'apps/api-identity/package.json', 'apps/api-identity/tsconfig*.json', 'packages/domain-types/src/**', 'packages/domain-types/package.json', 'packages/contracts/src/**', 'packages/contracts/package.json', 'packages/tsconfig/**', 'pnpm-lock.yaml') }}
      - name: Build api-identity Lambda package
        if: steps.cache-api-identity.outputs.cache-hit != 'true'
        run: bash scripts/package-lambda.sh
        working-directory: apps/api-identity

      # Also keyed on the 5 services' migrations/ directories, not just
      # migration-runner's own source — package-lambda.sh copies those
      # SQL files into the deployment package directly (see that
      # script's own header comment), so a migration added to any of
      # them needs a rebuild here too even if migration-runner's own
      # code didn't change. One caveat accepted for this one: a
      # cache-hit skips re-downloading the RDS CA bundle
      # (curl'd fresh in package-lambda.sh), so a cached build keeps
      # whichever bundle was embedded at cache-creation time — fine in
      # practice since AWS rotates that bundle on a multi-year cycle
      # with advance notice, not something that needs a same-day CI
      # rebuild to pick up.
      - name: Cache migration-runner Lambda package
        id: cache-migration-runner
        uses: actions/cache@v4
        with:
          path: apps/migration-runner/dist-lambda.zip
          key: migration-runner-lambda-${{ hashFiles('apps/migration-runner/src/**', 'apps/migration-runner/package.json', 'apps/migration-runner/tsconfig*.json', 'apps/api-catalog/migrations/**', 'apps/api-feed/migrations/**', 'apps/api-search/migrations/**', 'apps/api-commerce/migrations/**', 'apps/api-identity/migrations/**', 'packages/tsconfig/**', 'pnpm-lock.yaml') }}
      - name: Build migration-runner Lambda package
        if: steps.cache-migration-runner.outputs.cache-hit != 'true'
        run: bash scripts/package-lambda.sh
        working-directory: apps/migration-runner

      # Also caches a snapshot of .open-next/assets (not just the two
      # Lambda zips) — terraform-apply.yml's later "Sync OpenNext
      # static assets to S3" step needs that directory too, and it's
      # only ever produced by this same build. Snapshotted to its own
      # .open-next-assets directory rather than caching .open-next/assets
      # directly — see terraform-apply.yml's matching step for why
      # (the SG build below overwrites that path in place).
      - name: Cache apps/web OpenNext packages
        id: cache-web
        uses: actions/cache@v4
        with:
          path: |
            apps/web/dist-server-lambda.zip
            apps/web/dist-image-lambda.zip
            apps/web/.open-next-assets
          # web-opennext-3, matching terraform-apply.yml's own copy of
          # this key exactly (including the RAZORPAY_KEY_ID_PROD fold-in
          # and the -2 -> -3 bump for the .open-next-assets rename) -
          # see that workflow's comment for the full history. Keeping
          # both keys byte-identical matters: a cache entry created here
          # is exactly the one terraform-apply.yml reuses on the merge
          # commit, so a mismatched key would just force a rebuild there
          # instead of failing loudly - not dangerous, but defeats the
          # point of caching across the two workflows at all.
          key: web-opennext-3-${{ vars.RAZORPAY_KEY_ID_PROD }}-${{ hashFiles('apps/web/app/**', 'apps/web/src/**', 'apps/web/middleware.ts', 'apps/web/*.config.*', 'apps/web/package.json', 'apps/web/tsconfig*.json', 'packages/domain-types/src/**', 'packages/domain-types/package.json', 'packages/contracts/src/**', 'packages/contracts/package.json', 'packages/ui/src/**', 'packages/ui/package.json', 'packages/tsconfig/**', 'pnpm-lock.yaml') }}
      # Kept identical to terraform-apply.yml's own copy of this step
      # deliberately - the cache key above is hashFiles()-based (source
      # only, no env vars), so a build cached here without these could
      # get reused by terraform-apply.yml on the merge commit instead
      # of rebuilding with the correct values. See that workflow's copy
      # of this step for the full explanation.
      - name: Build apps/web OpenNext packages
        if: steps.cache-web.outputs.cache-hit != 'true'
        env:
          NEXT_PUBLIC_API_BASE_URL: https://api.puthagakadai.com
          NEXT_PUBLIC_CDN_HOST: cdn.puthagakadai.com
          NEXT_PUBLIC_RAZORPAY_KEY_ID: ${{ vars.RAZORPAY_KEY_ID_PROD }}
        run: |
          bash scripts/package-opennext.sh
          rm -rf .open-next-assets
          cp -r .open-next/assets .open-next-assets
        working-directory: apps/web

      # puthagakadai.sg — terraform plan needs both storefronts' zips to
      # exist too (web-sg.tf's own filebase64sha256() calls), same
      # reasoning as terraform-apply.yml's matching step, which this one
      # is kept identical to.
      - name: Cache apps/web OpenNext packages (SG)
        id: cache-web-sg
        uses: actions/cache@v4
        with:
          path: |
            apps/web/dist-server-lambda-sg.zip
            apps/web/dist-image-lambda-sg.zip
            apps/web/.open-next-sg-assets
          key: web-sg-opennext-1-${{ hashFiles('apps/web/app/**', 'apps/web/src/**', 'apps/web/middleware.ts', 'apps/web/*.config.*', 'apps/web/package.json', 'apps/web/tsconfig*.json', 'packages/domain-types/src/**', 'packages/domain-types/package.json', 'packages/contracts/src/**', 'packages/contracts/package.json', 'packages/ui/src/**', 'packages/ui/package.json', 'packages/tsconfig/**', 'pnpm-lock.yaml') }}
      - name: Build apps/web OpenNext packages (SG)
        if: steps.cache-web-sg.outputs.cache-hit != 'true'
        env:
          NEXT_PUBLIC_API_BASE_URL: https://api.puthagakadai.com
          NEXT_PUBLIC_CDN_HOST: cdn.puthagakadai.com
          NEXT_PUBLIC_FEATURE_PAY_LATER: "true"
          NEXT_PUBLIC_MARKET: SG
        run: |
          bash scripts/package-opennext.sh sg
          rm -rf .open-next-sg-assets
          cp -r .open-next/assets .open-next-sg-assets
        working-directory: apps/web

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9.8"

      - name: terraform fmt -check
        run: terraform fmt -check -recursive terraform/

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_DEPLOY_ROLE_ARN_PROD }}
          aws-region: ap-southeast-1

      - name: terraform init
        working-directory: terraform/environments/prod
        run: terraform init -input=false

      - name: terraform validate
        working-directory: terraform/environments/prod
        run: terraform validate

      - name: terraform plan
        working-directory: terraform/environments/prod
        # -parallelism=1: see terraform-apply.yml's matching comment —
        # this account's control-plane API endpoints have been observed
        # cross-wiring responses to the wrong concurrent request under
        # default parallelism (10), reproduced reliably across both
        # CloudShell and this workflow's own fresh OIDC session.
        run: terraform plan -input=false -out=tfplan -parallelism=1

      # Deliberately no `terraform apply` here — see terraform-apply.yml,
      # which only runs after merge to main.
